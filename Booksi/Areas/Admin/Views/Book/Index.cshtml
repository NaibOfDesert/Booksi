@model List<Book>

<h1> Category Index</h1>
<partial name="_Notification"/>

<div class="container"> 
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">
                Book List
            </h2>
        </div>
        <div class="col-6">
            <h2 class="text-end"> 
                <a asp-asp-area="Admin" asp-controller="Book" asp-action="Create" class="btn btn-primary">
                    <i class="bi bi-file-earmark-plus-fill">New Book</i> 
                </a>
            </h2>
        </div>


    </div>



</div>
<table class="table table-bordered table-striped"> 
    <thead>
        <tr> 
            <th> 
                Id
            <th>
                Title
            </th> 
            <th>
                Author
            </th>
            <th>
                ISBN
            </th>
            <th>
                Description
            </th> 
            <th>
                Price
            </th>
            <th>
                Extra Price
            </th>
            <th>
                
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach(var m in Model.OrderByDescending(x => x.Title)){
            <tr>
                <td>
                #@m.Id
                </td>
                <td>
                    @m.Title
                </td>
                <td>
                    @m.Author
                </td>
                <td>
                    @m.ISBN
                </td>
                <td>
                    @m.Description
                </td>
                <td>
                    @m.Price
                </td>
                <td>
                    @m.ExtraPrice
                </td>
                <td>
                    <div class="w-100 btn-group" role="group">

                    </div>
                    <a asp-asp-area="Admin" asp-controller="Book" asp-action="Edit" asp-route-id="@m.Id" class="btn btn-primary">
                        <i class="bi bi-pencil-square"></i>
                    </a>
                    <a asp-asp-area="Admin" asp-controller="Book" asp-action="Delete" asp-route-id="@m.Id" class="btn btn-primary">
                        <i class="bi bi-file-earmark-x"></i>
                    </a>
                </td>
            </tr>
        }
    </tbody>

</table>
